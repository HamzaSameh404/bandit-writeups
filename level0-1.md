# Bandit Level 0 → Level 1

## What the level wanted

Find the password stored in a file called `readme` in the home directory.

## Commands used

```bash
ls
cat readme
```

## Solution

Connected via SSH, listed files with `ls`, read the file with `cat readme`.

## Password found

ZjLjTmM6FvvyRnrb2rfNWOZOTa6ip5If

## What I learned

- How to connect to a remote server using **SSH**
- How to list files using **ls**
- How to read a file using **cat**
